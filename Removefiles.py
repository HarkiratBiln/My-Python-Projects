import os
import shutil
import time

def files():
    path = ("Files and folders to be deleted : ")

    days = 30
    seconds = time.time() - (days * 24 * 60 * 60)

    os.path.exists(path)




files()